import React from 'react';
import MenuCompleto from '@/components/MenuCompleto';

export const metadata = {
  title: 'Especialidades de Mariscos | Lapalapa',
  description: 'Descubre nuestras especialidades de mariscos frescos en Lapalapa.',
};

const EspecialidadesMariscosPage = () => {
  return <MenuCompleto seccionInicial="mariscos" />;
};

export default EspecialidadesMariscosPage;